/ make a donation

- form_tag :action => 'add_donation_to_cart', :redirect_to => (redirect_to ||= nil) do
  - if id
    = hidden_field_tag 'id', id
  %table.donationEntry
    %tr
      %td.label Donation amount: $
      %td
        = selects_for_dollar_amount('d', 4, 0)
        %span{:style=>"font-size: larger;"} .00
      %td= submit_tag 'Add Donation to Cart', :class => 'genButton'
