%label{:for => 'showdate_id'}  Reserve for:
- options = @valid_vouchers.empty? ? options_for_select(['(Select comp type first)']) : options_from_collection_for_select(@valid_vouchers, 'showdate_id', 'vouchertype_name_with_seats_of_type_remaining')
= select_tag('showdate_id', options)
%br
