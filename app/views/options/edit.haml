%p.infoMsg Click on the name of each configuration option for more information.
- form_tag 'edit' do 
  - @vars.each_pair do |group,varlist|
    .pageDiv
      %h1= h(group)
      %table
        - varlist.sort_by(&:id).each do |v|
          %tr{:class=>cycle(:evenRow,:oddRow)}
            %td.l= link_to_function h(Inflector.humanize(v.name)), "alert('#{escape_javascript(v.description)}')"
            - if v.typ == :int
              %td.l= text_field_tag "values[#{v.name}]", v.value, :size => 6, :maxlength => 6
            - else
              %td.l= text_field_tag "values[#{v.name}]", v.value, :size => 60
  .pageDiv
    %h1 Save Changes
    %p
      Clicking the button below will commit your changes, but it may take
      up to an hour for them to all take effect.  If you've changed your mind,
      =link_to "click here to cancel",:controller=>'customers',:action=>'welcome'
      and discard your changes.

    %p.c= submit_tag  "Update Settings"
