#select_show.form-inline
  %label.col-form-label Show and Date:
  = select_tag 'new_showdate', showdates_with_urls(walkup_sale_path(@showdate)), |
    :class => 'form-control', |
    :onchange => 'window.location = $("#new_showdate").val()' |
