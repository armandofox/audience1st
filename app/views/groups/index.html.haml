%h1 Listing Groups
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @groups.each do |group|
      %tr
        %td= group.name
        %td= link_to 'Show/Edit', group_path(group)
        %td= link_to 'Delete', delete_group_path(group), method: :delete, data: { confirm: 'Are you sure?' }
%br/
