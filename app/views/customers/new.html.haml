= themed

%h1.text-center Sign Up

%p.strong 
  Already have an account?
  = link_to 'Sign in', login_path
  to use your existing billing information.

= form_tag user_create_customers_path do
  = render :partial => 'billing_address', :locals => {:customer => @customer}
  .form-row
    .form-group.col-md-6
      %label.col-form-label{:for=>:customer_password}= defined?(newpassword) ? 'New Password' : 'Password'
      = password_field 'customer', 'password', :value => '', :autocomplete => 'off', :class => 'form-control form-control-sm'
    .form-group.col-md-6
      %label.col-form-label{:for=>:customer_password_confirmation}= defined?(newpassword) ? 'Confirm New Password' : 'Confirm Password'
      = password_field 'customer', 'password_confirmation', :value => '', :class => 'form-control form-control-sm'

  = render 'extra'
  .form-row
    .col-md-3= link_to 'Cancel', (@gOrderInProgress ? store_path : login_path), :class => 'btn btn-danger btn-block'
    .col-md-3.offset-md-6.text-right= submit_tag 'Create My Account', :class => 'btn btn-success btn-block'
