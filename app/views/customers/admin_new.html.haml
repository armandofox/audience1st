%h1.page_heading.center= Add Customer

#inner_wrapper
  = form_tag customers_path do
    = render 'billing_address'
    = render 'form_admin'
    = submit_tag 'Create Account'
    = link_to 'Cancel', customers_path, :class => 'genButton'
