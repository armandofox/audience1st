#   auto-generate database.yml 

base: &BASE
  adapter: mysql
  database: <%= venue %>
  host: localhost

production: 
  username: <%= venue %>
  password: <%= config['password'] %>
  <<: *BASE

migration:
  username: migration
  password: <%= migration_password %>
  <<: *BASE
